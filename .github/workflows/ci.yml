name: CI

on:
  push:
    branches:
      - main
  pull_request: ~
  workflow_dispatch: ~

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  tests:
    name: Tests
    runs-on: ubuntu-latest
    env:
      # The compose file defaults HTTP to 81 for local use; CI checks the standard port.
      HTTP_PORT: 80
      HTTPS_PORT: 443
      HTTP3_PORT: 443
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build Docker images
        uses: docker/bake-action@v6
        with:
          pull: true
          load: true
          files: |
            compose.yaml
            compose.override.yaml
          set: |
            *.cache-from=type=gha,scope=${{github.ref}}
            *.cache-from=type=gha,scope=refs/heads/main
            *.cache-to=type=gha,scope=${{github.ref}},mode=max
      - name: Start services
        run: docker compose up --wait --no-build
      - name: Check HTTP reachability
        run: curl -v -o /dev/null http://localhost
      - name: Check HTTPS reachability
        run: curl -vk -o /dev/null https://localhost
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: yarn
      - name: Test YARN
        # See the note in the Dockerfile: the lockfile format and packageManager disagree,
        # so --frozen-lockfile cannot be used yet.
        run: yarn install && yarn dev
      - name: Create test database
        run: docker compose exec -T php bin/console -e test doctrine:database:create
      - name: Run migrations
        run: docker compose exec -T php bin/console -e test doctrine:migrations:migrate --no-interaction
      - name: Load Fixtures
        run: docker compose exec -T php bin/console -e test hautelook:fixtures:load --no-interaction
      - name: Run PHPUnit
        run: docker compose exec -T php bin/phpunit
      - name: PHPStan
        # The container's CLI memory_limit is 128M, which PHPStan overruns on this codebase.
        run: docker compose exec -T php vendor/bin/phpstan analyse --memory-limit=-1
      - name: PHP-CS-Fixer
        run: docker compose exec -T php vendor/bin/php-cs-fixer fix --dry-run --diff
      - name: Doctrine Schema Validator
        run: docker compose exec -T php bin/console -e test doctrine:schema:validate

  lint:
    name: Docker Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Lint Dockerfile
        # v3.1.0 ships hadolint 2.12, whose parser rejects `COPY --exclude`.
        uses: hadolint/hadolint-action@v3.4.0
        with:
          # The upstream symfony-docker Dockerfile trips DL3025 (HEALTHCHECK shell
          # form) and DL3066 (`USER www-data`); upstream's own linter runs at `error`.
          failure-threshold: error
