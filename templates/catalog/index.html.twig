{% extends 'base.html.twig' %}

{% block body %}
<div class="container h-100">
    <div class="row">
        {% for support in supports %}
            <div class="col-md-6 col-lg-3">
                <a href="{{ path('app_catalog_support', {support: support.name}) }}" class="text-decoration-none">
                    <h1>{{ support.name | upper }}</h1>
                </a>
                <h1>
                </h1>
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
